--- 
site_name: TEEJAY VANSLYKE
title: Home
---

- content_for :header do
  %header.jumbotron.masthead#overview
    .inner
      .container
        %h1 Hi. I'm Teejay. I make things.
        %p I believe creativity is the cure for our society's consumer-oriented condition.

%section 
  .row
    .span12
      - @site.sorted_articles.each do |article|
        .post
          %h3
            %a{:href => article.path, :title => article[:title]}= article[:title]
            %span.date= article[:created_at].strftime('%Y-%m-%d %H:%M')
          .body= article.compiled_content
    .span4
      %h4 Music
      %ul.unstyled
        - @site.music_releases.each do |release|
          %li
            %a{:href => release[:url] ? release[:url] : release.path}= release[:title]
